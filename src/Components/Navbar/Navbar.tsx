import FiltersList from "../FiltersList/FiltersList";
import "./Navbar.scss";
const Navbar = () => {
  return (
    <div className="nav">
      <FiltersList />
    </div>
  );
};
export default Navbar;
